import React from 'react';
import './button.css'
export function Button(){
    return(
        <button id="btn-101">
        Glow Button
        <svg>
            <defs>
            <filter id="glow">
            
            </filter>
            </defs>
            <rect />
        </svg>
        </button>
    )
}